#include <stdio.h>

int main() {
    int n, i, count = 0;

    printf("Enter the value of n: ");
    scanf("%d", &n);

    printf("First %d odd numbers are:\n", n);

    i = 1; 
    while (count < n) {
        printf("%d\n", i);
        i = i + 2;
        count++;
    }
    return 0;
}
